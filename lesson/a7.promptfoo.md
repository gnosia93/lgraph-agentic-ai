## promptfoo로 LLM 성능 평가하기 ##

“프롬프트를 바꿨는데, 정말 더 나아진 걸까?” 생성형 AI 애플리케이션을 개발하는 엔지니어라면 누구나 한 번쯤 마주치는 질문입니다. 프롬프트 한 줄을 수정하거나 모델을 교체할 때마다 품질이 좋아졌는지 확신하기 어렵고, 여러 모델을 두고 어느 쪽이 서비스에 더 맞는지 판단하려면 같은 질문을 양쪽에 던져 결과를 일일이 눈으로 비교해야 합니다.

문제는 대규모 언어 모델(Large Language Model, 이하 LLM)이 비결정적(non-deterministic) 이라는 점입니다. 같은 입력에도 출력이 매번 달라지기 때문에, 전통적인 소프트웨어의 단위 테스트처럼 assertEquals 한 줄로 품질을 판정하기가 어렵습니다. 모델 스코어 카드만으로는 실제 응답 품질을 확신할 수 없기 때문에, 가장 확실한 방법은 동일한 테스트셋으로 직접 측정해 비교하는 것입니다. 다만 무엇을 어떻게 측정할지 정하는 것도, 평가 환경을 준비하는 것도 만만치 않습니다.

본 글에서는 오픈소스 LLM 평가 프레임워크인 promptfoo를 사용해 LLM 응답 품질을 손쉽게 평가하는 방법을 다룰 예정입니다. 평가 대상으로는 Amazon Bedrock에서 제공하는 모델 3종(Anthropic Claude Sonnet 4.5, Claude 3 Haiku, Amazon Nova Pro)을 사용하며, 채점용 모델(LLM judge)까지 Amazon Bedrock으로 구성하여, LLM 제공사별 API 인증 없이 평가 파이프라인을 완결하는 과정을 실제 예제와 함께 제시합니다.


## promptfoo 란? ##

promptfoo는 LLM을 위한 단위 테스트 프레임워크입니다. 소프트웨어 개발에서 코드를 변경할 때마다 단위 테스트를 돌려 로직이 제대로 동작하는지 확인하듯이, promptfoo는 프롬프트나 모델을 변경할 때마다 평가를 실행하여 응답 품질이 유지되는지 확인할 수 있게 해줍니다. 프롬프트, 비교할 모델 목록, 테스트 케이스를 하나의 YAML 파일에 선언적으로 정의하면, 여러 모델 × 여러 테스트 케이스 × 여러 평가 기준을 한 번에 실행하고 그 결과를 비교할 수 있습니다.

promptfoo 는 다음과 같은 장점을 제공합니다. 

* 로컬 우선(Local-first) 실행: 기본 실행이 로컬 환경에서 이루어지고 결과도 로컬에 저장됩니다. 테스트 데이터가 외부 SaaS로 전송되지 않아 보안·컴플라이언스 측면에서 유리합니다.
* 80+개 이상의 프로바이더 지원: OpenAI, Anthropic, Amazon Bedrock, Google Vertex AI, Ollama는 물론 임의의 HTTP 엔드포인트까지 동일한 방식으로 평가할 수 있습니다. (https://www.promptfoo.dev/docs/providers/)
* 선언적 설정: 프롬프트, 프로바이더, 테스트 케이스를 YAML 하나에 정의하므로, 설정 자체를 코드처럼 Git으로 버전 관리할 수 있습니다.
* CI/CD 친화적: promptfoo eval 명령은 테스트 실패 시 0이 아닌 종료 코드(exit code)를 반환합니다. 따라서 GitHub Actions 같은 파이프라인에 그대로 연결하여 품질 미달 시 빌드를 자동 중단시킬 수 있습니다.
* 시각화 리포트: promptfoo view 명령으로 모델별 응답, 차이(diff), 점수를 브라우저에서 한눈에 확인할 수 있습니다.


## promptfoo의 3요소 ##

promptfoo 는 설정 파일(promptfooconfig.yaml)을 이용하여 무엇을 묻고(prompts), 누구에게 묻고(providers), 어떻게 채점할지(tests) 라는 세 가지를 정의합니다.

![](https://github.com/gnosia93/lg-agentic-ai/blob/main/lesson/images/promptfoo_flow_pptx_white.png)

promptfoo의 평가 흐름은 다음과 같이 세 가지 구성 요소를 중심으로 동작합니다.

* prompts: 테스트할 프롬프트 템플릿입니다.  {{변수}} 형태로 정의해 두면, tests에 정의한 입력값이 그 자리에 채워져 실행 됩니다. 프롬프트를 여러 개 등록하면 각 프롬프트가 모든 모델에 대해 따로 평가되므로, "프롬프트 A와 B 중 어느 쪽이 더 나은가"를 비교할 수 있습니다.
* providers: 비교할 모델 목록입니다. 여기에 나열한 모델 수만큼 동일한 프롬프트가 각각 호출됩니다. 같은 모델을 temperature나 max_tokens 같은 파라미터만 바꿔 여러 번 등록하면, 파라미터가 결과에 미치는 영향도 비교할 수 있습니다.
* tests: 테스트 케이스입니다. 각 케이스는 프롬프트에 주입할 입력값(vars)과 그 응답을 검사할 평가 기준(assert)으로 이루어집니다. 모든 케이스에 공통으로 적용할 기준은 defaultTest에 한 번만 선언해 중복을 없앨 수 있고, 특정 케이스에만 필요한 기준은 그 케이스의 assert에 추가합니다.

위의 세 요소는 곱(조합)으로 동작합니다. 즉 프롬프트 수 × 모델(provider) 수 × 테스트 케이스 수만큼의 실행이 발생하며, 그 각각에 대해 assert가 채점됩니다. 예를 들어 프롬프트 1개, 모델 3개, 테스트 케이스 3개라면 총 9번(1 × 3 × 3)의 응답이 생성되고 채점됩니다. 이렇게 한 번의 eval 실행으로 여러 조합을 동시에 비교할 수 있다는 점이 promptfoo의 핵심입니다.



### 모델 평가 방식 ###
Promptfoo가 강력한 이유는 결정적(Deterministic) 방식과 모델 기반(Model-assisted) 평가 방식을 동시에 제공하기 때문입니다.
결정적 평가는 코드로 합격 여부를 판정하는 방식입니다. 특정 문자열 포함 여부(contains), JSON 형식 검증(is-json), 응답 시간 제한(latency), 비용 제한(cost) 등이 이에 해당하며, 명확하고 객관적인 평가 기준이 있을 때 매우 유용합니다.
반면, 모델 기반 평가는 평가 LLM(Judge LLM)이 다른 LLM의 출력을 채점하는 방식이며 llm-rubric이 대표적입니다. 자연어로 채점 기준을 작성하면 평가 LLM이 이를 해석해 Pass/Fail을 판정합니다. "답변이 친절한가?", "기술적으로 정확한가?"와 같은 주관적인 기준도 평가할 수 있어, 사람이 일일이 검수하는 수고를 획기적으로 줄여줍니다.
한 가지 유의할 점은 평가 LLM은 평가 대상 모델보다 성능이 뛰어난 '더 똑똑한 LLM'을 사용해야 한다는 것입니다. 예를 들어, 경량 모델인 Claude Haiku의 답변을 평가하기 위해, 최상위 모델인 Claude Opus를 평가 모델로 지정하는 방식입니다.

### 어떤 상황에서 유용한가? ###
* 프롬프트 엔지니어링: 프롬프트를 수정할 때마다 기존 품질이 유지되는지 자동 검증
* 모델 비교: 동일한 테스트셋으로 여러 모델의 품질, 비용, 속도를 객관적으로 비교
* CI/CD 통합: 프롬프트 변경을 코드 변경처럼 관리하고, PR(Pull Request) 마다 자동 평가 실행
* 레드팀 테스트: 67개 이상의 보안 공격 플러그인으로 LLM 취약점 스캔

Promptfoo의 가치는 LLM judge 자체보다는, 여러 모델 × 여러 테스트 케이스 × 여러 평가 기준을 한 번에 자동으로 돌리고 비교할 수 있는 프레임워크를 제공한다는 점이다.
이번 섹션에서는 Promptfoo 을 이용하여 AWS Bedrock 에서 제공되는 모델의 성능을 측정한다. 

### 1. 사전준비 ###
npm(Node Package Manager) 을 이용하여 promptfoo 및 AWS Bedrock SDK 를 설치한다.
```
npm install promptfoo
npm install @aws-sdk/client-bedrock-runtime

brew install node
node --version
```

### 2. AWS 인증 설정 ###
아래와 같이 AWS 인증 정보를 AWS CLI로 설정 또는 환경 변수로 설정한다. 
```
aws configure
```
또는
```
export AWS_ACCESS_KEY_ID="your_access_key"
export AWS_SECRET_ACCESS_KEY="your_secret_key"
export AWS_DEFAULT_REGION="us-east-1"
```

### 3. 지원 모델 확인 ###
AWS CLI 를 이용하여 설정된 리전을 확인한 후, 지원되는 모델 리스트를 확인한다. 
```
aws configure get region
aws bedrock list-foundation-models --query "modelSummaries[*].modelId" --output table
```
[결과]
```
--------------------------------------------------
|              ListFoundationModels              |
+------------------------------------------------+
|  twelvelabs.marengo-embed-2-7-v1:0             |
|  anthropic.claude-opus-4-5-20251101-v1:0       |
|  amazon.nova-2-lite-v1:0                       |
|  anthropic.claude-haiku-4-5-20251001-v1:0      |
|  anthropic.claude-opus-4-6-v1                  |
|  twelvelabs.pegasus-1-2-v1:0                   |
|  twelvelabs.marengo-embed-3-0-v1:0             |
|  anthropic.claude-sonnet-4-6                   |
|  cohere.embed-v4:0                             |
|  anthropic.claude-sonnet-4-5-20250929-v1:0     |
|  amazon.titan-embed-text-v2:0                  |
|  amazon.nova-pro-v1:0                          |
|  amazon.nova-lite-v1:0                         |
|  amazon.nova-micro-v1:0                        |
|  anthropic.claude-3-5-sonnet-20240620-v1:0     |
|  anthropic.claude-3-haiku-20240307-v1:0:200k   |
|  anthropic.claude-3-haiku-20240307-v1:0        |
|  anthropic.claude-3-sonnet-20240229-v1:0:28k   |
|  anthropic.claude-3-sonnet-20240229-v1:0:200k  |
|  anthropic.claude-3-sonnet-20240229-v1:0       |
|  anthropic.claude-3-5-sonnet-20241022-v2:0     |
|  anthropic.claude-3-7-sonnet-20250219-v1:0     |
|  anthropic.claude-sonnet-4-20250514-v1:0       |
+------------------------------------------------+
```

### 4. 프로젝트 초기화 ###
```
mkdir llm-eval
cd llm-eval
npx promptfoo init
```
* Compare prompts and models 메뉴을 선택한다.
    
![](https://github.com/gnosia93/lg-agentic-ai/blob/main/lesson/images/promptfoo-init-1.png)

* [AWS Bedrock] Claude, Llama, Titan .. 메뉴를 선택한다.  

![](https://github.com/gnosia93/lg-agentic-ai/blob/main/lesson/images/promptfoo-init-2.png)

* 프로젝트 디렉토리에 README.md 파일과 promptfootconfig.yaml 파일이 생성된 것을 확인한다.  

![](https://github.com/gnosia93/lg-agentic-ai/blob/main/lesson/images/promptfoo-init-3.png)

* 생성된 promptfooconfig.yaml 설정 파일을 확인한다.
  
![](https://github.com/gnosia93/lg-agentic-ai/blob/main/lesson/images/promptfoo-init-4.png)

### 5. 평가 모델(Judge 모델) 선정 ###

모델 채점용(llm-rubric) 으로 haiku 모델을 사용할 예정이다. 아래 명령어로 동작여부를 체크한다.
```
 aws bedrock-runtime invoke-model \
  --model-id apac.anthropic.claude-3-haiku-20240307-v1:0 \
  --region ap-northeast-2 \
  --content-type application/json \
  --body '{"anthropic_version":"bedrock-2023-05-31","max_tokens":100,"messages":[{"role":"user","content":"hello"}]}' \
  /tmp/response.json
```
[결과]
```
{
    "contentType": "application/json"
}
```


### 6. 테스트 시나리오 작성 ###
```
cat <<EOF > promptfooconfig.yaml
description: AWS Bedrock 모델 비교 평가

prompts:
  - |
    다음 질문에 한국어로 답변해줘.
    정확하고 간결하게 답변할 것.

    질문: {{question}}

providers:
  # Claude Sonnet 4
  - id: bedrock:apac.anthropic.claude-sonnet-4-20250514-v1:0
    config:
      region: ap-northeast-2
      temperature: 0
      max_tokens: 1024

  - id: bedrock:apac.anthropic.claude-3-haiku-20240307-v1:0
    config:
      region: ap-northeast-2
      temperature: 0
      max_tokens: 1024

  # Amazon Nova Pro
  - id: bedrock:apac.amazon.nova-pro-v1:0
    config:
      region: ap-northeast-2
      interfaceConfig:
        temperature: 0
        max_new_tokens: 1024

# 모든 test에 적용되는 공통 테스트 기준을 명시.
defaultTest:   
  options:
    provider:   # 평가 LLM(judge LLM) 설정
      id: bedrock:apac.anthropic.claude-3-haiku-20240307-v1:0
      config:
        region: ap-northeast-2
        temperature: 0
  assert:
    # cost 는 지원하지 않으므로 주석처리한다. 
    #- type: cost
    #  threshold: 0.05
    - type: latency
      threshold: 10000
    # 한국어로 답변했는가
    - type: llm-rubric
      value: "답변이 한국어로 작성되어 있는가?"
      metric: 언어
    # 기술적 정확성
    - type: llm-rubric
      value: "기술적으로 틀린 내용이 없는가?"
      metric: 정확성
    # 구조화된 답변인가
    - type: llm-rubric
      value: "답변이 논리적으로 구조화되어 있고, 핵심을 먼저 말하는가?"
      metric: 구조
    # 실용성
    - type: llm-rubric
      value: "실무에서 바로 활용할 수 있는 구체적인 예시나 코드가 포함되어 있는가?"
      metric: 실용성
    # 간결성 (너무 길지 않은가)
    - type: javascript
      value: "output.length < 3000 ? 1.0 : Math.max(0, 1 - (output.length - 3000) / 5000)"
      metric: 간결성

tests:
  - vars:
      question: "쿠버네티스에서 파드와 디플로이먼트의 차이점은?"
    threshold: 0.7
    assert:         # 아래 기준 1개 + defaultTest 6개에 대해서 대해서 test 를 수행한다.      
      - type: llm-rubric
        value: "파드와 디플로이먼트의 차이를 정확히 설명하고, 실무 관점의 예시가 포함되어 있는가?"
    
  - vars:
      question: "REST API와 GraphQL의 장단점을 비교해줘"
    threshold: 0.7
    assert:
      - type: llm-rubric
        value: "두 기술의 장단점이 균형 있게 설명되어 있는가?"
      - type: contains
        value: "GraphQL"

  - vars:
      question: "Docker 컨테이너와 가상머신의 차이는?"
    threshold: 0.7
    assert:
      - type: llm-rubric
        value: "기술적으로 정확하고, 초보자도 이해할 수 있는 설명인가?"
EOF
```
promptfoo 의 테스트 시나리오는 다음과 같이 총 4개의 섹션으로 이뤄져 있다.

#### 6-1. 기본 정보 및 프롬프트 정의 ####
* description: 이 테스트 프로젝트의 목적을 나타내는 이름입니다. 웹 대시보드나 결과 리포트에 표시됩니다.
* prompts: 테스트 대상 모델들에게 입력할 공통 프롬프트(지시문)입니다. {{question}} 부분에 하단의 테스트 케이스들이 변수로 주입됩니다.

#### 6-2. 평가 대상 모델 (Providers) 설정 ####
* providers: 답변 품질을 비교할 AWS Bedrock의 모델 목록입니다. (Claude Sonnet 4, Claude 3 Haiku, Amazon Nova Pro 총 3종)
* config: 각 모델의 호출 옵션입니다. 모든 모델의 temperature를 0으로 설정하여 답변의 일관성(무작위성 배제)을 확보하고, 아시아 태평양(서울) 리전(ap-northeast-2)을 바라보도록 설정했습니다. Amazon Nova 모델의 경우 토큰 및 온도를 설정하는 하위 속성명이 interfaceConfig로 정의되어 있습니다.
* max_tokens - 모델이 한 번에 생성할 수 있는 최대 출력 토큰 수

#### 6-3. 공통 검증 기준 (Default Test) ####
이 섹션은 모든 테스트에서 공통적으로 적용되는 테스트 케이스를 작성하는 부분이다. 테스트 케이스를 나열한 tests 섹션의 "쿠버네티스에서 파드와 디플로이먼트의 차이점은?"
의 경우 설정상 하나의 assert 만 있지만 defaultTest 에 설정된 6개의 항목과 더불어서 총 7개의 테스트가 실행되게 된다.

#### 6-4. 개별 테스트 케이스 (Tests) ####
질문 데이터와 해당 질문에만 적용되는 특화 검증 기준입니다. 모든 테스트는 threshold: 0.7 조건이 있어, 공통 기준과 개별 기준을 합산한 점수가 70% 이상이어야 합격(Pass) 처리됩니다.
이 예제의 경우  쿠버네티스 질문, REST API vs GraphQL 질문 그리고  Docker vs VM 질문으로 구성되어져 있으며 각기 다른 평가 기준을 가지고 있다.
threshold 0.7 의 경우 다음과 같은 의미를 가지게 된다. << 추가 한다. >> 

### 7. 테스트 ###
eval 옵션으로 테스트 케이스를 실행한다.
```
npx promptfoo@latest eval
```
[결과]
![](https://github.com/gnosia93/eks-agentic-ai/blob/main/lesson/images/promptfoo-result.png)

웹브라우저 리포트를 실행하여 테스트 케이스에 대한 상세 내용을 조회한다.
```
npx promptfoo@latest view
```
[결과]
![](https://github.com/gnosia93/eks-agentic-ai/blob/main/lesson/images/promptfoo-view.png)

### 8. 테스트 케이스(Assert 타입 정리) ###

#### 결정적(Deterministic) 평가 — LLM 없이 코드로 판정 ####

| 카테고리 | assert 타입 | 설명 |
|---------|------------|------|
| 문자열 | equals | 정확히 일치 |
| | contains | 특정 문자열 포함 |
| | icontains | 포함 (대소문자 무시) |
| | contains-any | 목록 중 하나라도 포함 |
| | contains-all | 목록 전부 포함 |
| | starts-with | 특정 문자열로 시작 |
| | regex | 정규식 매칭 |
| 형식 검증 | is-json | 유효한 JSON (스키마 검증 가능) |
| | contains-json | JSON 포함 여부 |
| | is-html | 유효한 HTML |
| | is-sql | 유효한 SQL |
| | is-xml | 유효한 XML |
| 성능 | latency | 응답 시간 임계값 (ms) |
| | cost | 비용 임계값 ($) |
| | perplexity | 퍼플렉시티 임계값 |
| 텍스트 유사도 | rouge-n | ROUGE-N 점수 |
| | bleu | BLEU 점수 |
| | levenshtein | 편집 거리 |
| | meteor | METEOR 점수 |
| 커스텀 | javascript | JS 함수로 판정 |
| | python | Python 함수로 판정 |
| | webhook | 외부 웹훅으로 판정 |
| 기타 | is-refusal | 모델이 거부 응답을 했는가 |
| 에이전트 | trajectory:tool-used | 특정 도구를 호출했는가 |
| | trajectory:tool-sequence | 도구 호출 순서가 맞는가 |
| | trajectory:tool-args-match | 도구 호출 인자가 맞는가 |
| | trajectory:step-count | 스텝 수 검증 |

#### 모델 기반(Model-assisted) 평가 — LLM이 판정 ####

| 카테고리 | assert 타입 | 설명 |
|---------|------------|------|
| 범용 | llm-rubric | 자유 기준으로 LLM이 채점 |
| | g-eval | Chain-of-thought 기반 평가 |
| | similar | 임베딩 코사인 유사도 |
| | classifier | 분류기로 판정 |
| | select-best | 여러 출력 중 최고 선택 |
| | factuality | 사실 관계 검증 |
| | model-graded-closedqa | OpenAI Closed QA 방식 평가 |
| RAG 특화 | answer-relevance | 답변이 질문과 관련 있는가 |
| | context-faithfulness | 답변이 컨텍스트에 충실한가 |
| | context-recall | 정답이 컨텍스트에 포함되어 있는가 |
| | context-relevance | 컨텍스트가 질문과 관련 있는가 |
| 에이전트 | trajectory:goal-success | 에이전트가 목표를 달성했는가 |
| 대화 | conversation-relevance | 대화 흐름에서 응답이 관련 있는가 |

> 모든 타입 앞에 `not-`을 붙이면 반대로 동작한다. 예: `not-contains`, `not-regex`



## 레퍼런스 ##
* https://github.com/promptfoo/promptfoo 
